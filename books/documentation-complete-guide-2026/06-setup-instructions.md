---
title: "セットアップ手順の明確化"
---

# 第6章: セットアップ手順の明確化

## この章で学ぶこと

この章では、誰でも迷わず環境構築できるセットアップ手順の書き方を学びます。優れたプロジェクトであっても、セットアップで躓いてしまえば、そこで終わりです。初めて触れる開発者が、スムーズに動作環境を構築し、最初の成功体験を得られるような手順書の作成方法を習得します。

具体的には以下の内容をカバーします：

- **前提条件の明確化**: 必要な知識、ツール、環境を明示する方法
- **ステップバイステップの手順**: 誰でも迷わず進められる具体的な手順
- **トラブルシューティング**: よくあるエラーとその解決方法
- **動作確認方法**: セットアップが正しく完了したことを確認する手段
- **プラットフォーム別の対応**: macOS、Linux、Windowsそれぞれへの配慮

セットアップ手順書は、プロジェクトへの「入り口」です。この入り口が狭く複雑であれば、多くの開発者は入ってきません。この章を通じて、開放的で分かりやすい入り口を作る技術を身につけましょう。

## なぜセットアップ手順の明確化が重要か

### 第一印象を決める

セットアップ手順は、開発者がプロジェクトに対して持つ第一印象を決定づけます。

```markdown
# ❌ 悪い例：手順が不明確

## Installation

Install dependencies and run the project.

\`\`\`bash
npm install
npm start
\`\`\`
```

```markdown
# ✅ 良い例：手順が明確で親切

## セットアップ手順

### 前提条件

以下がインストールされていることを確認してください：

- Node.js 18.x 以上
- npm 9.x 以上

Node.jsのバージョン確認：

\`\`\`bash
node --version  # v18.0.0 以上であることを確認
npm --version   # v9.0.0 以上であることを確認
\`\`\`

### 1. リポジトリのクローン

\`\`\`bash
git clone https://github.com/example/project.git
cd project
\`\`\`

### 2. 依存関係のインストール

\`\`\`bash
npm install
\`\`\`

インストールには約2分かかります。

### 3. 環境変数の設定

\`\`\`bash
cp .env.example .env
\`\`\`

`.env`ファイルを開き、以下の値を設定してください：

- `DATABASE_URL`: データベース接続文字列
- `API_KEY`: APIキー（開発環境では`dev-key-12345`を使用可能）

### 4. 開発サーバーの起動

\`\`\`bash
npm run dev
\`\`\`

ブラウザで http://localhost:3000 を開いてください。
「Welcome」画面が表示されれば成功です。
```

### 離脱率を下げる

不明確なセットアップ手順は、開発者の離脱につながります。

一般的に、セットアップで15分以上躓くと、多くの開発者がプロジェクトを諦める傾向があります。明確な手順書があれば、この離脱を防げます。

### サポートコストを削減する

よくある質問に先回りして答えることで、サポートにかかる時間を削減できます。

```markdown
# ✅ よくあるエラーを先回りして記載

### トラブルシューティング

#### `npm install`が失敗する

**エラーメッセージ:**
```
Error: EACCES: permission denied
```

**解決方法:**

1. sudoを使わずにnpmをインストールし直してください
2. または、npmのグローバルディレクトリを変更してください

詳細: https://docs.npmjs.com/resolving-eacces-permissions-errors

#### ポート3000が既に使用されている

**エラーメッセージ:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解決方法:**

別のポートを指定してください：

\`\`\`bash
PORT=3001 npm run dev
\`\`\`
```

### 信頼性を示す

詳細で正確なセットアップ手順は、プロジェクト全体の品質を示すシグナルになります。

## 前提条件の明記

### なぜ前提条件が重要か

前提条件を明記しないと、「動かない」という問題の切り分けができません。

```markdown
# ❌ 悪い例：前提条件がない

## Setup

\`\`\`bash
npm install
npm start
\`\`\`
```

このような手順書では、以下の問題が発生します：

- Node.jsがインストールされていない
- バージョンが古すぎる（または新しすぎる）
- 必要な外部サービス（データベース等）が準備されていない
- OSによる差異が考慮されていない

### 前提条件チェックリストの作成

必要な前提条件を網羅的にリストアップします。

```markdown
# ✅ 良い例：包括的な前提条件

## 前提条件

### 必須

- **Node.js**: 18.0.0 以上
- **npm**: 9.0.0 以上
- **Git**: 2.30.0 以上
- **データベース**: PostgreSQL 14.x 以上（ローカルまたはリモート）

### 推奨

- **OS**: macOS 12以上、Ubuntu 20.04以上、Windows 11（WSL2使用）
- **メモリ**: 8GB以上
- **ディスク空き容量**: 5GB以上

### 事前準備

- GitHubアカウント
- PostgreSQLのデータベース作成権限
- （本番環境の場合）AWS CLIのセットアップと認証情報

### バージョン確認方法

\`\`\`bash
# Node.js
node --version

# npm
npm --version

# Git
git --version

# PostgreSQL
psql --version
\`\`\`

期待される出力：

\`\`\`
v18.17.0  # または18.x以上
9.8.1     # または9.x以上
git version 2.39.2
psql (PostgreSQL) 14.9
\`\`\`
```

### 前提知識の明示

技術的な前提知識も明記することで、学習者が適切な準備をできます。

```markdown
## 前提知識

このプロジェクトをセットアップする前に、以下の知識があることが推奨されます：

### 必須の知識

- **コマンドライン操作**: ターミナルでの基本的なコマンド実行
- **Git の基本**: clone、pull、commitの概念
- **Node.js/npm の基本**: package.jsonの役割、依存関係の概念

### あると望ましい知識

- **REST APIの基本**: HTTPメソッド（GET、POST等）の理解
- **SQLの基本**: SELECT、INSERT文の記述経験
- **環境変数の概念**: .envファイルの役割

### 初心者向けリソース

上記の知識がない場合は、以下のリソースで学習してください：

- コマンドライン: [Command Line Crash Course](https://example.com)
- Git基礎: [Git Handbook](https://guides.github.com/introduction/git-handbook/)
- Node.js入門: [Node.js公式ガイド](https://nodejs.org/en/docs/guides/getting-started-guide/)
```

### プラットフォーム別の前提条件

OSによって前提条件が異なる場合は、明確に区別します。

```markdown
## プラットフォーム別の前提条件

### macOS

\`\`\`bash
# Homebrewのインストール（未インストールの場合）
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Node.jsのインストール
brew install node@18

# PostgreSQLのインストール
brew install postgresql@14
brew services start postgresql@14
\`\`\`

### Ubuntu/Debian

\`\`\`bash
# Node.jsのインストール
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# PostgreSQLのインストール
sudo apt-get install postgresql-14
sudo systemctl start postgresql
\`\`\`

### Windows

Windows上での開発にはWSL2（Windows Subsystem for Linux 2）の使用を推奨します。

1. WSL2のインストール（管理者権限のPowerShellで実行）：

\`\`\`powershell
wsl --install
\`\`\`

2. 再起動後、Ubuntu 22.04をインストール
3. 以降はUbuntuの手順に従ってください

**注意**: ネイティブWindowsでも動作しますが、一部のnpmパッケージでビルドエラーが発生する可能性があります。
```

## ステップバイステップの手順

### 手順設計の原則

効果的なセットアップ手順は、以下の原則に従います：

1. **順序性**: 各ステップは順番に実行できる
2. **原子性**: 各ステップは1つの明確な目的を持つ
3. **検証可能性**: 各ステップの完了を確認できる
4. **回復可能性**: 失敗しても前のステップに戻れる

### 基本的な手順構造

```markdown
# ✅ 良い例：明確な手順構造

## セットアップ手順

以下の手順は約10分で完了します。

### ステップ1: リポジトリの取得

**目的**: プロジェクトのソースコードを取得します。

\`\`\`bash
git clone https://github.com/example/project.git
cd project
\`\`\`

**確認方法**:

\`\`\`bash
ls -la
\`\`\`

以下のファイルが表示されれば成功です：
- `package.json`
- `src/`
- `README.md`

### ステップ2: 依存関係のインストール

**目的**: プロジェクトに必要なnpmパッケージをインストールします。

\`\`\`bash
npm install
\`\`\`

このステップは約2-3分かかります。完了すると`node_modules/`ディレクトリが作成されます。

**確認方法**:

\`\`\`bash
npm list --depth=0
\`\`\`

インストールされたパッケージのリストが表示されれば成功です。

**トラブルシューティング**:

- エラーが発生した場合は、[トラブルシューティング](#トラブルシューティング)セクションを参照してください
```

### コピペ可能なコマンド

すべてのコマンドは、そのままコピー&ペーストで実行できる形式にします。

```markdown
# ❌ 悪い例：コピペできない

環境変数を設定してください。DATABASE_URLには接続文字列を、API_KEYにはあなたのキーを入れてください。
```

```markdown
# ✅ 良い例：コピペ可能

### ステップ3: 環境変数の設定

\`\`\`bash
# テンプレートをコピー
cp .env.example .env

# エディタで開く（お好みのエディタを使用）
nano .env
# または
code .env
# または
vim .env
\`\`\`

`.env`ファイルを以下のように編集してください：

\`\`\`bash
# データベース接続
DATABASE_URL="postgresql://localhost:5432/myapp_dev"

# APIキー（開発環境用のデフォルト値）
API_KEY="dev-key-12345"

# サーバー設定
PORT=3000
NODE_ENV=development
\`\`\`

**開発環境での注意事項**:

- `API_KEY`は開発環境では`dev-key-12345`で問題ありません
- 本番環境では必ず実際のAPIキーに変更してください
```

### 実行時間の目安

各ステップにかかる時間の目安を示します。

```markdown
## セットアップ時間の目安

| ステップ | 所要時間 | 備考 |
|---------|---------|------|
| 1. リポジトリのクローン | 30秒 | ネットワーク速度に依存 |
| 2. 依存関係のインストール | 2-3分 | 初回は時間がかかります |
| 3. データベースのセットアップ | 1分 | PostgreSQL起動済みの場合 |
| 4. 環境変数の設定 | 2分 | 手動編集が必要 |
| 5. 開発サーバーの起動 | 30秒 | ビルドが完了するまで |

**合計**: 約6-8分
```

### 成功の確認方法

各ステップでの成功を明確に確認できるようにします。

```markdown
### ステップ6: 動作確認

**開発サーバーを起動**:

\`\`\`bash
npm run dev
\`\`\`

以下の出力が表示されれば成功です：

\`\`\`
> project@1.0.0 dev
> next dev

ready - started server on 0.0.0.0:3000, url: http://localhost:3000
info  - Loaded env from .env
event - compiled client and server successfully in 2.3s
\`\`\`

**ブラウザで確認**:

1. ブラウザを開く
2. http://localhost:3000 にアクセス
3. 以下の画面が表示されることを確認：

![セットアップ成功画面](./images/setup-success.png)

**APIの動作確認**:

\`\`\`bash
curl http://localhost:3000/api/health
\`\`\`

期待されるレスポンス：

\`\`\`json
{
  "status": "ok",
  "database": "connected",
  "version": "1.0.0"
}
\`\`\`

すべて確認できれば、セットアップは完了です！
```

## プラットフォーム別の対応

### なぜプラットフォーム別対応が必要か

開発者が使用するOSは多様です。macOS、Linux、Windowsそれぞれで異なる手順が必要な場合、明確に区別する必要があります。

### タブ形式での表示

同じ目的の異なる手順を、プラットフォーム別に整理します。

```markdown
## インストール手順

以下のタブから、お使いのOSを選択してください。

### macOS

\`\`\`bash
# Homebrewでのインストール
brew install node@18 postgresql@14

# サービスの起動
brew services start postgresql@14
\`\`\`

### Linux (Ubuntu/Debian)

\`\`\`bash
# Node.jsのインストール
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# PostgreSQLのインストール
sudo apt-get install postgresql-14
sudo systemctl start postgresql
sudo systemctl enable postgresql
\`\`\`

### Windows (WSL2)

\`\`\`bash
# WSL2のUbuntu内で実行
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs postgresql-14

# サービスの起動
sudo service postgresql start
\`\`\`

**WSL2のセットアップがまだの場合**:

1. PowerShell（管理者権限）で実行：
   \`\`\`powershell
   wsl --install
   \`\`\`
2. 再起動後、Ubuntu 22.04をインストール
3. 上記のLinuxの手順を実行
```

### プラットフォーム固有の注意事項

各プラットフォームで特別な注意が必要な点を明記します。

```markdown
## プラットフォーム別の注意事項

### macOS

#### Apple Silicon (M1/M2/M3) の場合

一部のnpmパッケージはネイティブモジュールを含み、Apple Silicon用に再ビルドが必要な場合があります。

\`\`\`bash
# Rosetta 2が必要な場合（通常は不要）
softwareupdate --install-rosetta
\`\`\`

#### Intel Mac の場合

特別な設定は不要です。通常の手順でセットアップできます。

### Linux

#### ファイルウォッチャーの制限

大規模なプロジェクトでは、ファイルウォッチャーの上限を増やす必要があります：

\`\`\`bash
echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
\`\`\`

#### SELinuxの設定

SELinuxが有効な場合（RHEL/CentOS/Fedora等）、ポートアクセスの設定が必要です：

\`\`\`bash
sudo semanage port -a -t http_port_t -p tcp 3000
\`\`\`

### Windows

#### WSL2の推奨理由

ネイティブWindowsでも動作しますが、以下の理由でWSL2を推奨します：

- ファイルシステムのパフォーマンスが優れている
- npmパッケージの互換性が高い
- 本番環境（Linux）との差異が少ない

#### ネイティブWindowsでの実行

WSL2を使用しない場合：

1. Node.js Windowsインストーラーを使用
2. PostgreSQLのWindowsバイナリをインストール
3. 環境変数はシステム設定から設定

**注意**: 一部のnpmスクリプトがWindows環境で動作しない可能性があります。
```

### パスの表記

プラットフォームによって異なるパスの表記を明確にします。

```markdown
## プロジェクトの配置場所

### macOS / Linux

\`\`\`bash
# ホームディレクトリ配下を推奨
cd ~/projects
git clone https://github.com/example/project.git
cd project
\`\`\`

### Windows (WSL2)

\`\`\`bash
# Linuxファイルシステム内を推奨（高速）
cd ~/projects
git clone https://github.com/example/project.git
cd project
\`\`\`

**重要**: Windowsファイルシステム（`/mnt/c/`配下）にクローンすると、
ファイルアクセスが遅くなります。WSL2のLinuxファイルシステムを使用してください。

### Windows (ネイティブ)

\`\`\`powershell
# ユーザーディレクトリ配下
cd C:\Users\YourName\projects
git clone https://github.com/example/project.git
cd project
\`\`\`
```

## データベース・外部サービスのセットアップ

### データベースのセットアップ

データベースが必要なプロジェクトでは、その準備手順を明確にします。

```markdown
## データベースのセットアップ

### PostgreSQLのインストール確認

\`\`\`bash
psql --version
\`\`\`

`psql (PostgreSQL) 14.x`と表示されれば、インストール済みです。

### データベースの作成

\`\`\`bash
# PostgreSQLのCLIに接続
psql postgres

# データベース作成（psql内で実行）
CREATE DATABASE myapp_dev;

# ユーザー作成（必要に応じて）
CREATE USER myapp_user WITH PASSWORD 'dev_password';

# 権限付与
GRANT ALL PRIVILEGES ON DATABASE myapp_dev TO myapp_user;

# 接続確認
\c myapp_dev

# 終了
\q
\`\`\`

### マイグレーションの実行

\`\`\`bash
# テーブル作成
npm run db:migrate

# 初期データ投入（開発環境のみ）
npm run db:seed
\`\`\`

**確認方法**:

\`\`\`bash
psql myapp_dev -c "\dt"
\`\`\`

以下のテーブルが表示されれば成功です：

\`\`\`
           List of relations
 Schema |       Name       | Type  |    Owner
--------+------------------+-------+--------------
 public | users            | table | myapp_user
 public | posts            | table | myapp_user
 public | schema_migrations| table | myapp_user
\`\`\`
```

### Dockerを使用した簡易セットアップ

Dockerが利用できる環境では、より簡単なセットアップを提供します。

```markdown
## Dockerを使用したセットアップ（推奨）

Dockerがインストールされている場合、以下の方法が最も簡単です。

### 前提条件

- Docker 20.10以上
- Docker Compose 2.0以上

### セットアップ手順

\`\`\`bash
# すべてのサービスを起動（データベース、Redis等）
docker-compose up -d

# 起動確認
docker-compose ps
\`\`\`

期待される出力：

\`\`\`
NAME                COMMAND                  SERVICE     STATUS
project-db-1        "docker-entrypoint.s…"   db          Up
project-redis-1     "docker-entrypoint.s…"   redis       Up
\`\`\`

### マイグレーションの実行

\`\`\`bash
# コンテナ内でマイグレーション実行
docker-compose exec app npm run db:migrate
docker-compose exec app npm run db:seed
\`\`\`

### アプリケーションの起動

\`\`\`bash
# 開発サーバー起動
npm run dev
\`\`\`

アプリケーションはDocker内のデータベースに自動接続します。

### 停止方法

\`\`\`bash
# すべてのサービスを停止
docker-compose down

# データも削除する場合
docker-compose down -v
\`\`\`
```

### 外部サービスの準備

外部API等が必要な場合の準備手順も記載します。

```markdown
## 外部サービスの設定

### APIキーの取得

このプロジェクトは以下の外部サービスを使用します：

#### 1. OpenAI API（必須）

1. https://platform.openai.com/ にアクセス
2. アカウント作成（まだの場合）
3. API Keysセクションで「Create new secret key」をクリック
4. 生成されたキーをコピー（一度しか表示されません）
5. `.env`ファイルの`OPENAI_API_KEY`に設定

\`\`\`bash
OPENAI_API_KEY="sk-proj-xxxxxxxxxxxxxxxxxxxx"
\`\`\`

#### 2. Stripe（決済機能を使用する場合のみ）

開発環境では、テストキーを使用します：

1. https://dashboard.stripe.com/test/apikeys にアクセス
2. 「Publishable key」と「Secret key」をコピー
3. `.env`ファイルに設定

\`\`\`bash
STRIPE_PUBLIC_KEY="pk_test_xxxxxxxxxxxxxxxxxxxx"
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxxxxxxxxxx"
\`\`\`

**注意**: テストキーでは実際の決済は発生しません。

### 開発環境でのモック使用

外部サービスなしで開発する場合、モックモードが使用できます：

\`\`\`bash
# .envファイルに追加
USE_MOCK_APIS=true
\`\`\`

この設定により、外部APIを呼び出さずにダミーレスポンスを返します。
```

## トラブルシューティング

### トラブルシューティングセクションの重要性

よくあるエラーとその解決策を先回りして記載することで、開発者の時間を大幅に節約できます。

### エラーパターンの分類

エラーを以下のカテゴリに分類して整理します：

1. **依存関係のインストールエラー**
2. **権限エラー**
3. **ポート競合エラー**
4. **データベース接続エラー**
5. **環境変数エラー**
6. **ビルド・コンパイルエラー**

### 依存関係のインストールエラー

```markdown
## トラブルシューティング

### 依存関係のインストールに失敗する

#### エラー1: `EACCES: permission denied`

**エラーメッセージ:**

\`\`\`
npm ERR! code EACCES
npm ERR! syscall mkdir
npm ERR! path /usr/local/lib/node_modules
npm ERR! errno -13
npm ERR! Error: EACCES: permission denied, mkdir '/usr/local/lib/node_modules'
\`\`\`

**原因**: npmのグローバルディレクトリに書き込み権限がありません。

**解決方法**:

❌ **推奨しない**: `sudo npm install`を使用する

✅ **推奨**: npmのグローバルディレクトリを変更する

\`\`\`bash
# npmの設定変更
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'

# PATHに追加（~/.bashrc または ~/.zshrc に追記）
export PATH=~/.npm-global/bin:$PATH

# 設定を反映
source ~/.bashrc  # または source ~/.zshrc

# 再度インストール
npm install
\`\`\`

参考: https://docs.npmjs.com/resolving-eacces-permissions-errors

#### エラー2: `ERESOLVE unable to resolve dependency tree`

**エラーメッセージ:**

\`\`\`
npm ERR! code ERESOLVE
npm ERR! ERESOLVE unable to resolve dependency tree
\`\`\`

**原因**: パッケージ間の依存関係の競合です。

**解決方法**:

\`\`\`bash
# レガシーモードでインストール
npm install --legacy-peer-deps

# または、強制的にインストール
npm install --force
\`\`\`

**注意**: `--force`は依存関係の問題を無視するため、動作に問題が出る可能性があります。
まずは`--legacy-peer-deps`を試してください。

#### エラー3: `gyp ERR! build error`

**エラーメッセージ:**

\`\`\`
gyp ERR! build error
gyp ERR! stack Error: `make` failed with exit code: 2
\`\`\`

**原因**: ネイティブモジュールのビルドに必要なツールがありません。

**解決方法 (macOS)**:

\`\`\`bash
# Xcode Command Line Toolsをインストール
xcode-select --install
\`\`\`

**解決方法 (Ubuntu/Debian)**:

\`\`\`bash
# ビルドツールをインストール
sudo apt-get install build-essential python3
\`\`\`

**解決方法 (Windows)**:

\`\`\`powershell
# Visual Studio Build Toolsをインストール
npm install --global windows-build-tools
\`\`\`
```

### ポート競合エラー

```markdown
### ポートが既に使用されている

#### エラー: `EADDRINUSE`

**エラーメッセージ:**

\`\`\`
Error: listen EADDRINUSE: address already in use :::3000
\`\`\`

**原因**: ポート3000が既に別のプロセスで使用されています。

**解決方法1: 使用中のプロセスを終了する**

macOS/Linux:

\`\`\`bash
# ポート3000を使用しているプロセスを確認
lsof -i :3000

# プロセスを終了（PIDは上記コマンドの出力から取得）
kill -9 [PID]
\`\`\`

Windows:

\`\`\`powershell
# ポート3000を使用しているプロセスを確認
netstat -ano | findstr :3000

# プロセスを終了（PIDは上記コマンドの出力から取得）
taskkill /PID [PID] /F
\`\`\`

**解決方法2: 別のポートを使用する**

\`\`\`bash
# .envファイルでポートを変更
PORT=3001

# または、コマンドラインで指定
PORT=3001 npm run dev
\`\`\`
```

### データベース接続エラー

```markdown
### データベースに接続できない

#### エラー1: `Connection refused`

**エラーメッセージ:**

\`\`\`
Error: connect ECONNREFUSED 127.0.0.1:5432
\`\`\`

**原因**: PostgreSQLが起動していません。

**解決方法**:

macOS (Homebrew):

\`\`\`bash
# PostgreSQLを起動
brew services start postgresql@14

# 起動確認
brew services list | grep postgresql
\`\`\`

Linux:

\`\`\`bash
# PostgreSQLを起動
sudo systemctl start postgresql

# 起動確認
sudo systemctl status postgresql
\`\`\`

Windows (WSL2):

\`\`\`bash
# PostgreSQLを起動
sudo service postgresql start

# 起動確認
sudo service postgresql status
\`\`\`

#### エラー2: `Authentication failed`

**エラーメッセージ:**

\`\`\`
error: password authentication failed for user "myapp_user"
\`\`\`

**原因**: データベースの認証情報が正しくありません。

**解決方法**:

1. `.env`ファイルの`DATABASE_URL`を確認
2. パスワードが正しいか確認
3. ユーザーが作成されているか確認

\`\`\`bash
# PostgreSQLに接続してユーザー確認
psql postgres
\du

# ユーザーが存在しない場合は作成
CREATE USER myapp_user WITH PASSWORD 'dev_password';
GRANT ALL PRIVILEGES ON DATABASE myapp_dev TO myapp_user;
\`\`\`

#### エラー3: `database does not exist`

**エラーメッセージ:**

\`\`\`
error: database "myapp_dev" does not exist
\`\`\`

**原因**: データベースが作成されていません。

**解決方法**:

\`\`\`bash
# データベースを作成
psql postgres -c "CREATE DATABASE myapp_dev;"

# 作成確認
psql postgres -c "\l" | grep myapp_dev
\`\`\`
```

### 環境変数エラー

```markdown
### 環境変数が読み込まれない

#### エラー: `process.env.XXX is undefined`

**原因**: 環境変数が正しく設定されていません。

**チェックリスト**:

1. [ ] `.env`ファイルが存在するか確認
2. [ ] `.env`ファイルがプロジェクトルートにあるか確認
3. [ ] `.env`ファイルの形式が正しいか確認
4. [ ] サーバーを再起動したか確認

**正しい .env ファイルの形式**:

\`\`\`bash
# ✅ 正しい形式
DATABASE_URL="postgresql://localhost:5432/myapp_dev"
API_KEY="dev-key-12345"
PORT=3000

# ❌ 間違った形式（空白に注意）
DATABASE_URL = "postgresql://localhost:5432/myapp_dev"  # = の前後に空白
API_KEY = dev-key-12345  # 引用符なし（値に空白がなければOKだが、統一推奨）
\`\`\`

**環境変数の確認方法**:

\`\`\`bash
# Node.jsアプリケーション内で確認
node -e "require('dotenv').config(); console.log(process.env.DATABASE_URL)"
\`\`\`

期待される出力: `postgresql://localhost:5432/myapp_dev`

出力が`undefined`の場合、`.env`ファイルが読み込まれていません。
```

### ビルド・コンパイルエラー

```markdown
### ビルドが失敗する

#### エラー1: `TypeScript compilation errors`

**エラーメッセージ:**

\`\`\`
error TS2304: Cannot find name 'XXX'.
\`\`\`

**原因**: 型定義が不足しているか、TypeScriptの設定に問題があります。

**解決方法**:

\`\`\`bash
# 型定義を再インストール
rm -rf node_modules package-lock.json
npm install

# TypeScriptの型定義を明示的にインストール
npm install --save-dev @types/node @types/react
\`\`\`

#### エラー2: `Module not found`

**エラーメッセージ:**

\`\`\`
Error: Cannot find module 'XXX'
\`\`\`

**原因**: 依存関係が正しくインストールされていません。

**解決方法**:

\`\`\`bash
# node_modules を削除して再インストール
rm -rf node_modules package-lock.json
npm install

# キャッシュをクリア
npm cache clean --force
npm install
\`\`\`

#### エラー3: `Out of memory`

**エラーメッセージ:**

\`\`\`
FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
\`\`\`

**原因**: Node.jsのメモリ上限に達しました。

**解決方法**:

\`\`\`bash
# メモリ上限を増やして実行
NODE_OPTIONS="--max-old-space-size=4096" npm run build

# package.json のスクリプトに追記（恒久的な対応）
{
  "scripts": {
    "build": "NODE_OPTIONS='--max-old-space-size=4096' next build"
  }
}
\`\`\`
```

### プラットフォーム固有のエラー

```markdown
### macOS固有の問題

#### Apple Silicon (M1/M2/M3) でのエラー

**エラー: `Unsupported architecture`**

一部のnpmパッケージがApple Siliconに対応していない場合があります。

**解決方法**:

\`\`\`bash
# Rosetta 2経由でインストール
arch -x86_64 npm install
\`\`\`

### Linux固有の問題

#### ファイルウォッチャーの上限エラー

**エラーメッセージ:**

\`\`\`
ENOSPC: System limit for number of file watchers reached
\`\`\`

**解決方法**:

\`\`\`bash
# ファイルウォッチャーの上限を増やす
echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
\`\`\`

### Windows固有の問題

#### パスの長さ制限

**エラー: `ENAMETOOLONG`**

Windowsのパス長制限（260文字）に引っかかる場合があります。

**解決方法**:

1. プロジェクトをCドライブ直下に配置（`C:\project`等）
2. または、長いパスのサポートを有効化（Windows 10 1607以降）

レジストリエディタで以下を設定：

\`\`\`
キー: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\FileSystem
値: LongPathsEnabled (DWORD)
データ: 1
\`\`\`
```

## 自動化とセットアップスクリプト

### セットアップスクリプトの必要性

複雑なセットアップ手順は、スクリプト化することで自動化できます。

```markdown
## クイックスタート（自動セットアップ）

以下のスクリプトで、すべてのセットアップを自動実行できます。

### 前提条件

- Node.js 18.x 以上
- PostgreSQL 14.x 以上（起動済み）

### 自動セットアップの実行

\`\`\`bash
# セットアップスクリプトを実行
npm run setup
\`\`\`

このスクリプトは以下を自動実行します：

1. 依存関係のインストール
2. 環境変数ファイルの作成（`.env.example`からコピー）
3. データベースの作成
4. マイグレーションの実行
5. 初期データの投入
6. 動作確認

**実行結果の例**:

\`\`\`
🔍 Checking prerequisites...
✓ Node.js 18.17.0 detected
✓ PostgreSQL 14.9 detected

📦 Installing dependencies...
✓ Dependencies installed (127 packages)

📝 Setting up environment variables...
✓ .env file created

🗄️  Setting up database...
✓ Database 'myapp_dev' created
✓ Migrations applied (3 migrations)
✓ Seed data inserted

🚀 Starting development server...
✓ Server running at http://localhost:3000

✨ Setup complete! Open http://localhost:3000 in your browser.
\`\`\`
```

### セットアップスクリプトの実装例

```markdown
## セットアップスクリプトの詳細

`npm run setup`は以下のスクリプトを実行します。

### package.json

\`\`\`json
{
  "scripts": {
    "setup": "node scripts/setup.js",
    "setup:db": "node scripts/setup-database.js",
    "db:create": "createdb myapp_dev",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed"
  }
}
\`\`\`

### scripts/setup.js

\`\`\`javascript
#!/usr/bin/env node

const { execSync } = require('child_process');
const fs = require('fs');
const path = require('path');

// ログ用のユーティリティ
const log = {
  info: (msg) => console.log(`ℹ️  ${msg}`),
  success: (msg) => console.log(`✅ ${msg}`),
  error: (msg) => console.error(`❌ ${msg}`),
  section: (msg) => console.log(`\n${'='.repeat(50)}\n${msg}\n${'='.repeat(50)}`)
};

// コマンド実行のヘルパー
function exec(command, options = {}) {
  try {
    execSync(command, {
      stdio: options.silent ? 'pipe' : 'inherit',
      ...options
    });
    return true;
  } catch (error) {
    if (!options.ignoreError) {
      throw error;
    }
    return false;
  }
}

// 前提条件のチェック
function checkPrerequisites() {
  log.section('前提条件のチェック');

  // Node.jsのバージョン確認
  const nodeVersion = process.version;
  const majorVersion = parseInt(nodeVersion.slice(1).split('.')[0]);

  if (majorVersion < 18) {
    log.error(`Node.js 18.x以上が必要です（現在: ${nodeVersion}）`);
    process.exit(1);
  }
  log.success(`Node.js ${nodeVersion} を検出`);

  // PostgreSQLの確認
  const pgInstalled = exec('which psql', { silent: true, ignoreError: true });
  if (!pgInstalled) {
    log.error('PostgreSQLがインストールされていません');
    log.info('インストール方法: https://www.postgresql.org/download/');
    process.exit(1);
  }
  log.success('PostgreSQL を検出');
}

// 依存関係のインストール
function installDependencies() {
  log.section('依存関係のインストール');
  log.info('npm install を実行中... (数分かかる場合があります)');

  exec('npm install');
  log.success('依存関係をインストール完了');
}

// 環境変数ファイルの作成
function setupEnvironment() {
  log.section('環境変数の設定');

  const envPath = path.join(process.cwd(), '.env');
  const envExamplePath = path.join(process.cwd(), '.env.example');

  if (fs.existsSync(envPath)) {
    log.info('.env ファイルは既に存在します（スキップ）');
    return;
  }

  if (!fs.existsSync(envExamplePath)) {
    log.error('.env.example ファイルが見つかりません');
    process.exit(1);
  }

  fs.copyFileSync(envExamplePath, envPath);
  log.success('.env ファイルを作成');
  log.info('必要に応じて .env ファイルを編集してください');
}

// データベースのセットアップ
function setupDatabase() {
  log.section('データベースのセットアップ');

  // データベースの作成（既に存在する場合はエラーを無視）
  log.info('データベースを作成中...');
  exec('npm run db:create', { ignoreError: true });

  // マイグレーション
  log.info('マイグレーションを実行中...');
  exec('npm run db:migrate');
  log.success('マイグレーション完了');

  // シードデータ投入
  log.info('初期データを投入中...');
  exec('npm run db:seed');
  log.success('初期データ投入完了');
}

// 動作確認
function verifySetup() {
  log.section('動作確認');

  // 環境変数のチェック
  require('dotenv').config();

  if (!process.env.DATABASE_URL) {
    log.error('DATABASE_URL が設定されていません');
    process.exit(1);
  }
  log.success('環境変数の確認完了');

  // その他の確認...
}

// メイン処理
async function main() {
  console.log('\n🚀 セットアップを開始します\n');

  try {
    checkPrerequisites();
    installDependencies();
    setupEnvironment();
    setupDatabase();
    verifySetup();

    log.section('セットアップ完了！');
    console.log(`
次のコマンドで開発サーバーを起動してください:

    npm run dev

ブラウザで http://localhost:3000 を開いてください。
    `);

  } catch (error) {
    log.error('セットアップに失敗しました');
    console.error(error);
    process.exit(1);
  }
}

main();
\`\`\`

### スクリプトの特徴

- **前提条件の自動チェック**: Node.js、PostgreSQLのバージョンを確認
- **わかりやすいログ**: 絵文字とメッセージで進行状況を明示
- **エラーハンドリング**: 適切なエラーメッセージとリカバリー手順
- **冪等性**: 複数回実行しても安全
```

### インタラクティブなセットアップ

ユーザーに質問しながらセットアップを進める方法もあります。

```markdown
## インタラクティブセットアップ

質問に答えながらセットアップを進めることもできます。

\`\`\`bash
npm run setup:interactive
\`\`\`

**セットアップの流れ**:

\`\`\`
? プロジェクト名を入力してください: my-awesome-app
? データベース名を入力してください: (my-awesome-app_dev)
? ポート番号を指定してください: (3000)
? OpenAI APIキーを入力してください: sk-proj-xxxxx
? Stripeを使用しますか? (y/N) n
? 初期データを投入しますか? (Y/n) y

✓ 設定を保存しました
✓ データベースを作成しました
✓ マイグレーションを実行しました
✓ 初期データを投入しました

🎉 セットアップ完了！
\`\`\`

### 実装（Inquirer.jsを使用）

\`\`\`javascript
const inquirer = require('inquirer');
const fs = require('fs');

async function interactiveSetup() {
  const answers = await inquirer.prompt([
    {
      type: 'input',
      name: 'projectName',
      message: 'プロジェクト名を入力してください:',
      default: 'my-app'
    },
    {
      type: 'input',
      name: 'databaseName',
      message: 'データベース名を入力してください:',
      default: (answers) => `${answers.projectName}_dev`
    },
    {
      type: 'number',
      name: 'port',
      message: 'ポート番号を指定してください:',
      default: 3000
    },
    {
      type: 'password',
      name: 'openaiApiKey',
      message: 'OpenAI APIキーを入力してください:',
      mask: '*'
    },
    {
      type: 'confirm',
      name: 'useStripe',
      message: 'Stripeを使用しますか?',
      default: false
    },
    {
      type: 'confirm',
      name: 'seedData',
      message: '初期データを投入しますか?',
      default: true
    }
  ]);

  // .envファイルを生成
  const envContent = `
DATABASE_URL="postgresql://localhost:5432/${answers.databaseName}"
PORT=${answers.port}
OPENAI_API_KEY="${answers.openaiApiKey}"
${answers.useStripe ? `STRIPE_SECRET_KEY=""` : '# Stripe is disabled'}
  `.trim();

  fs.writeFileSync('.env', envContent);

  // 以降のセットアップ処理...
}
\`\`\`
```

## 開発環境と本番環境の違い

### 環境別のセットアップ

開発環境と本番環境でセットアップ手順が異なる場合、明確に区別します。

```markdown
## 環境別セットアップ

### 開発環境（ローカル）

開発環境では、以下の簡易設定を使用できます：

\`\`\`bash
# .env（開発環境）
NODE_ENV=development
DATABASE_URL="postgresql://localhost:5432/myapp_dev"
API_KEY="dev-key-12345"  # 開発用のダミーキー
REDIS_URL="redis://localhost:6379"
LOG_LEVEL="debug"  # 詳細なログを出力
\`\`\`

**特徴**:
- ダミーのAPIキーが使用可能
- 詳細なログ出力
- ホットリロード有効
- ソースマップ有効

### ステージング環境

ステージング環境は本番に近い設定を使用します：

\`\`\`bash
# .env.staging
NODE_ENV=production
DATABASE_URL="postgresql://staging-db.example.com:5432/myapp_staging"
API_KEY="${STAGING_API_KEY}"  # 環境変数から取得
REDIS_URL="redis://staging-redis.example.com:6379"
LOG_LEVEL="info"
\`\`\`

**デプロイ手順**:

\`\`\`bash
# ビルド
npm run build

# ステージング環境へデプロイ
npm run deploy:staging
\`\`\`

### 本番環境

本番環境では、セキュリティと性能を重視した設定を使用します：

\`\`\`bash
# .env.production（実際はシークレット管理サービスを使用）
NODE_ENV=production
DATABASE_URL="postgresql://prod-db.example.com:5432/myapp_prod"
API_KEY="${PROD_API_KEY}"  # シークレット管理サービスから取得
REDIS_URL="redis://prod-redis.example.com:6379"
LOG_LEVEL="error"  # エラーのみログ出力
SESSION_SECRET="${SESSION_SECRET}"  # 必ず変更
\`\`\`

**デプロイ手順**:

\`\`\`bash
# ビルド（本番用最適化）
npm run build:production

# テスト実行
npm run test

# 本番環境へデプロイ
npm run deploy:production
\`\`\`

**本番環境の注意事項**:

- [ ] 環境変数は必ず AWS Secrets Manager 等のシークレット管理サービスを使用
- [ ] データベース接続はSSL必須
- [ ] APIキーは絶対にコミットしない
- [ ] セッションシークレットは強固なランダム文字列を使用
- [ ] ログレベルは`error`または`warn`に設定
```

### Docker Composeでの環境別設定

```markdown
## Docker Composeを使用した環境管理

### 開発環境

\`\`\`yaml
# docker-compose.yml
version: '3.8'

services:
  app:
    build:
      context: .
      target: development
    volumes:
      - .:/app  # ホットリロード用
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://postgres:password@db:5432/myapp_dev
    depends_on:
      - db

  db:
    image: postgres:14
    environment:
      POSTGRES_DB: myapp_dev
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
\`\`\`

### 本番環境

\`\`\`yaml
# docker-compose.prod.yml
version: '3.8'

services:
  app:
    build:
      context: .
      target: production
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}  # 外部から注入
      - API_KEY=${API_KEY}
    restart: always

  # 本番ではマネージドサービスを使用するため、dbサービスは含めない
\`\`\`

### 起動方法

\`\`\`bash
# 開発環境
docker-compose up

# 本番環境
docker-compose -f docker-compose.prod.yml up -d
\`\`\`
```

## 動作確認とヘルスチェック

### 基本的な動作確認

セットアップが正しく完了したことを確認する方法を明示します。

```markdown
## 動作確認

セットアップが正しく完了したことを確認しましょう。

### 1. サーバーの起動確認

\`\`\`bash
npm run dev
\`\`\`

以下の出力が表示されれば成功です：

\`\`\`
> project@1.0.0 dev
> next dev

ready - started server on 0.0.0.0:3000, url: http://localhost:3000
event - compiled client and server successfully
\`\`\`

### 2. ブラウザでの確認

ブラウザで http://localhost:3000 を開き、以下を確認：

- [ ] ページが正しく表示される
- [ ] コンソールにエラーが出ていない
- [ ] ネットワークタブで4xxエラーが出ていない

### 3. APIエンドポイントの確認

\`\`\`bash
# ヘルスチェックエンドポイント
curl http://localhost:3000/api/health

# 期待されるレスポンス
{
  "status": "ok",
  "timestamp": "2026-01-28T12:00:00.000Z",
  "uptime": 123.456,
  "database": "connected",
  "redis": "connected"
}
\`\`\`

### 4. データベース接続の確認

\`\`\`bash
# データベースに直接接続
psql $DATABASE_URL -c "SELECT version();"

# または、アプリケーション経由で確認
npm run db:check
\`\`\`

期待される出力：

\`\`\`
PostgreSQL 14.9 on x86_64-apple-darwin, compiled by Apple clang version 14.0.0
\`\`\`

### 5. 自動テストの実行

\`\`\`bash
# すべてのテストを実行
npm run test

# 期待される出力
PASS  src/lib/utils.test.ts
PASS  src/api/users.test.ts
PASS  src/api/posts.test.ts

Test Suites: 3 passed, 3 total
Tests:       24 passed, 24 total
\`\`\`

すべてのテストがパスすれば、セットアップは完了です！
```

### ヘルスチェックエンドポイントの実装

```markdown
## ヘルスチェックの実装

セットアップ確認用のヘルスチェックエンドポイントを実装する例：

### pages/api/health.ts

\`\`\`typescript
import { NextApiRequest, NextApiResponse } from 'next';
import { PrismaClient } from '@prisma/client';
import Redis from 'ioredis';

const prisma = new PrismaClient();
const redis = new Redis(process.env.REDIS_URL);

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse
) {
  const startTime = Date.now();

  try {
    // データベース接続確認
    await prisma.$queryRaw`SELECT 1`;
    const dbStatus = 'connected';

    // Redis接続確認
    await redis.ping();
    const redisStatus = 'connected';

    // 応答時間の計算
    const responseTime = Date.now() - startTime;

    res.status(200).json({
      status: 'ok',
      timestamp: new Date().toISOString(),
      uptime: process.uptime(),
      responseTime: `${responseTime}ms`,
      database: dbStatus,
      redis: redisStatus,
      environment: process.env.NODE_ENV,
      version: process.env.npm_package_version
    });
  } catch (error) {
    res.status(503).json({
      status: 'error',
      message: error instanceof Error ? error.message : 'Unknown error',
      timestamp: new Date().toISOString()
    });
  }
}
\`\`\`

### ヘルスチェックスクリプト

\`\`\`bash
#!/bin/bash
# scripts/health-check.sh

ENDPOINT="http://localhost:3000/api/health"
MAX_RETRIES=5
RETRY_INTERVAL=2

echo "🏥 ヘルスチェックを実行中..."

for i in $(seq 1 $MAX_RETRIES); do
  echo "試行 $i/$MAX_RETRIES..."

  RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" $ENDPOINT)

  if [ "$RESPONSE" = "200" ]; then
    echo "✅ サーバーは正常に動作しています"
    curl -s $ENDPOINT | jq .
    exit 0
  fi

  echo "❌ サーバーが応答しません（ステータス: $RESPONSE）"

  if [ $i -lt $MAX_RETRIES ]; then
    echo "⏳ ${RETRY_INTERVAL}秒後に再試行..."
    sleep $RETRY_INTERVAL
  fi
done

echo "❌ ヘルスチェックに失敗しました"
exit 1
\`\`\`

### package.json に追加

\`\`\`json
{
  "scripts": {
    "health": "bash scripts/health-check.sh"
  }
}
\`\`\`

### 使用方法

\`\`\`bash
# 開発サーバー起動後
npm run health
\`\`\`
```

## 次のステップへの導線

```markdown
## 次のステップ

セットアップが完了したら、以下のドキュメントを参照して開発を始めましょう。

### 開発を始める

- **チュートリアル**: [docs/tutorial.md](./docs/tutorial.md)
  - 最初のAPIエンドポイントを作成
  - データベースとの連携
  - フロントエンドとの統合

- **開発ガイド**: [docs/development.md](./docs/development.md)
  - コーディング規約
  - ディレクトリ構造
  - 推奨するツール

### API仕様を確認する

- **API仕様書**: [docs/api/README.md](./docs/api/README.md)
- **OpenAPI**: http://localhost:3000/api-docs（開発サーバー起動後）

### テストを書く

- **テストガイド**: [docs/testing.md](./docs/testing.md)
  - ユニットテストの書き方
  - 統合テストの実行方法
  - E2Eテストのセットアップ

### デプロイする

- **デプロイガイド**: [docs/deployment.md](./docs/deployment.md)
  - Vercelへのデプロイ
  - AWS EC2へのデプロイ
  - Dockerでのデプロイ

### 困ったときは

- **FAQ**: [docs/faq.md](./docs/faq.md)
- **トラブルシューティング**: このドキュメントの[トラブルシューティング](#トラブルシューティング)セクション
- **Issue**: [GitHub Issues](https://github.com/example/project/issues)
- **Discord**: [コミュニティサーバー](https://discord.gg/example)
```

## 実践的なパターン集

### パターン1: モノレポのセットアップ

```markdown
## モノレポのセットアップ

このプロジェクトはモノレポ構造を採用しています。

### ディレクトリ構造

\`\`\`
project/
├── packages/
│   ├── web/          # Next.jsアプリケーション
│   ├── api/          # Express APIサーバー
│   ├── shared/       # 共通ライブラリ
│   └── mobile/       # React Nativeアプリ
├── package.json      # ルートのpackage.json
└── pnpm-workspace.yaml
\`\`\`

### セットアップ手順

このプロジェクトはpnpm workspacesを使用しています。

#### 1. pnpmのインストール

\`\`\`bash
npm install -g pnpm@8
\`\`\`

#### 2. すべてのパッケージをインストール

\`\`\`bash
# ルートディレクトリで実行
pnpm install
\`\`\`

pnpmは自動的にすべてのワークスペースの依存関係をインストールします。

#### 3. 各パッケージのビルド

\`\`\`bash
# 共通ライブラリをビルド（他のパッケージが依存）
pnpm --filter shared build

# すべてのパッケージをビルド
pnpm -r build
\`\`\`

#### 4. 開発サーバーの起動

すべてのパッケージを同時に起動：

\`\`\`bash
pnpm dev
\`\`\`

または、個別に起動：

\`\`\`bash
# Webアプリのみ
pnpm --filter web dev

# APIサーバーのみ
pnpm --filter api dev
\`\`\`

### パッケージ別のセットアップ

各パッケージには個別の`.env`ファイルがあります：

\`\`\`bash
# Webアプリの環境変数
cp packages/web/.env.example packages/web/.env

# APIサーバーの環境変数
cp packages/api/.env.example packages/api/.env
\`\`\`

### よくあるエラー

#### ワークスペース間の依存関係エラー

**エラー**: `Cannot find module '@myapp/shared'`

**解決方法**:

\`\`\`bash
# 共通ライブラリを再ビルド
pnpm --filter shared build

# キャッシュをクリア
pnpm store prune
pnpm install
\`\`\`
```

### パターン2: マイクロサービスのセットアップ

```markdown
## マイクロサービスアーキテクチャのセットアップ

このプロジェクトは以下のマイクロサービスで構成されています：

- **user-service**: ユーザー管理（Port 3001）
- **auth-service**: 認証・認可（Port 3002）
- **post-service**: 投稿管理（Port 3003）
- **api-gateway**: APIゲートウェイ（Port 3000）

### 前提条件

- Docker Desktop 20.10以上
- Docker Compose 2.0以上

### セットアップ手順

#### 1. すべてのサービスを起動

\`\`\`bash
docker-compose up -d
\`\`\`

これにより、すべてのマイクロサービスとインフラ（PostgreSQL、Redis等）が起動します。

#### 2. 起動確認

\`\`\`bash
docker-compose ps
\`\`\`

すべてのサービスが`Up`状態であることを確認：

\`\`\`
NAME                    STATUS              PORTS
api-gateway             Up 10 seconds       0.0.0.0:3000->3000/tcp
user-service            Up 11 seconds       0.0.0.0:3001->3001/tcp
auth-service            Up 11 seconds       0.0.0.0:3002->3002/tcp
post-service            Up 11 seconds       0.0.0.0:3003->3003/tcp
postgres                Up 12 seconds       0.0.0.0:5432->5432/tcp
redis                   Up 12 seconds       0.0.0.0:6379->6379/tcp
\`\`\`

#### 3. 各サービスのマイグレーション

\`\`\`bash
# user-service
docker-compose exec user-service npm run db:migrate

# post-service
docker-compose exec post-service npm run db:migrate
\`\`\`

#### 4. 動作確認

\`\`\`bash
# APIゲートウェイ経由で各サービスを確認
curl http://localhost:3000/users/health
curl http://localhost:3000/auth/health
curl http://localhost:3000/posts/health
\`\`\`

すべて`{"status":"ok"}`が返れば成功です。

### 個別サービスの開発

特定のサービスのみをローカルで開発する場合：

\`\`\`bash
# user-serviceをローカルで起動
cd services/user-service
npm install
npm run dev

# 他のサービスはDocker Composeで起動
docker-compose up -d auth-service post-service api-gateway postgres redis
\`\`\`

### サービス間通信の確認

\`\`\`bash
# ログを確認
docker-compose logs -f api-gateway

# 特定のサービスのログ
docker-compose logs -f user-service
\`\`\`
```

### パターン3: フロントエンド + バックエンドのセットアップ

```markdown
## フルスタックアプリケーションのセットアップ

このプロジェクトはフロントエンド（React）とバックエンド（Node.js）で構成されています。

### ディレクトリ構造

\`\`\`
project/
├── client/           # Reactアプリケーション（Port 3000）
│   ├── package.json
│   └── src/
├── server/           # Express APIサーバー（Port 3001）
│   ├── package.json
│   └── src/
└── docker-compose.yml
\`\`\`

### セットアップ方法

#### 方法1: 自動セットアップ（推奨）

ルートディレクトリで実行：

\`\`\`bash
npm run setup
\`\`\`

このスクリプトは自動的に以下を実行します：
1. クライアントとサーバーの依存関係インストール
2. データベースのセットアップ
3. 環境変数ファイルの作成

#### 方法2: 手動セットアップ

**ステップ1: サーバーのセットアップ**

\`\`\`bash
cd server
npm install

# 環境変数を設定
cp .env.example .env

# データベースのセットアップ
npm run db:setup

# 開発サーバー起動
npm run dev
\`\`\`

サーバーは http://localhost:3001 で起動します。

**ステップ2: クライアントのセットアップ**

別のターミナルで：

\`\`\`bash
cd client
npm install

# 環境変数を設定
cp .env.example .env

# 開発サーバー起動
npm run dev
\`\`\`

クライアントは http://localhost:3000 で起動します。

### 同時起動

ルートディレクトリから両方を同時起動：

\`\`\`bash
# ルートディレクトリで
npm run dev
\`\`\`

このコマンドは`concurrently`を使用してクライアントとサーバーを同時起動します。

### 環境変数の設定

#### server/.env

\`\`\`bash
PORT=3001
DATABASE_URL="postgresql://localhost:5432/myapp_dev"
JWT_SECRET="your-secret-key"
CLIENT_URL="http://localhost:3000"
\`\`\`

#### client/.env

\`\`\`bash
REACT_APP_API_URL="http://localhost:3001"
\`\`\`

### プロキシ設定

開発環境では、CORSの問題を避けるためにプロキシを設定します。

#### client/package.json に追加

\`\`\`json
{
  "proxy": "http://localhost:3001"
}
\`\`\`

これにより、クライアントから`/api/`へのリクエストが自動的にサーバーに転送されます。

### 動作確認

1. ブラウザで http://localhost:3000 を開く
2. ログイン画面が表示されることを確認
3. APIが正しく動作することを確認：

\`\`\`bash
curl http://localhost:3001/api/health
\`\`\`
```

## セットアップドキュメントのメンテナンス

### ドキュメントの更新

セットアップ手順は、プロジェクトの変更に応じて定期的に更新が必要です。

```markdown
## セットアップドキュメントの更新ガイドライン

### 更新が必要なタイミング

- [ ] 新しい依存関係を追加したとき
- [ ] Node.jsやデータベースのバージョン要件が変わったとき
- [ ] 環境変数が追加・変更されたとき
- [ ] セットアップ手順が変更されたとき
- [ ] よくあるエラーが新たに報告されたとき

### 更新の手順

1. **変更を文書化**

\`\`\`markdown
## 変更履歴

### 2026-01-28
- Node.js 16.x から 18.x に要件を変更
- PostgreSQL 13.x から 14.x に更新
- `.env.example`に`REDIS_URL`を追加
\`\`\`

2. **実際にセットアップを実行**

ドキュメントを更新する前に、実際に手順を試してください：

\`\`\`bash
# クリーンな環境で試す
rm -rf node_modules package-lock.json .env
# ドキュメント通りにセットアップ
\`\`\`

3. **レビューを依頼**

特に初心者の視点でレビューしてもらうことが重要です。

### バージョン管理

セットアップドキュメントにもバージョンを記載します：

\`\`\`markdown
# セットアップガイド

**ドキュメントバージョン**: 2.1.0
**対応プロジェクトバージョン**: v3.0.0以降
**最終更新**: 2026-01-28

---
\`\`\`
```

### 定期的な検証

```markdown
## セットアップドキュメントの検証

### 自動化されたセットアップテスト

CI/CDパイプラインで、セットアップ手順が実際に動作することを確認します。

#### .github/workflows/setup-test.yml

\`\`\`yaml
name: Setup Documentation Test

on:
  pull_request:
    paths:
      - 'README.md'
      - 'docs/setup.md'
      - 'package.json'
      - '.env.example'

jobs:
  test-setup:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node-version: [18, 20]

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup PostgreSQL
        uses: ikalnytskyi/action-setup-postgres@v4

      - name: Run setup script
        run: npm run setup

      - name: Verify setup
        run: |
          npm run health
          npm run test

      - name: Comment on PR
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: 'セットアップドキュメントのテストが失敗しました。手順を確認してください。'
            })
\`\`\`

### 手動検証チェックリスト

新しいバージョンをリリースする前に、以下を確認：

- [ ] クリーンな環境でセットアップを実行
- [ ] macOS、Linux、Windows（WSL2）で動作確認
- [ ] 初心者にセットアップを試してもらう
- [ ] よくあるエラーセクションが最新
- [ ] スクリーンショットやコマンド出力が最新
- [ ] リンク切れがない
- [ ] タイポがない
```

## この章のまとめ

```markdown
## まとめ

この章では、誰でも迷わず環境構築できるセットアップ手順の書き方を学びました。

### 重要なポイント

1. **前提条件の明確化**
   - 必要なツール、バージョン、知識を明示
   - プラットフォーム別の要件を区別

2. **ステップバイステップの手順**
   - コピペで実行できるコマンド
   - 各ステップの目的と確認方法
   - 実行時間の目安

3. **トラブルシューティング**
   - よくあるエラーパターンを網羅
   - エラーメッセージと解決策を対応付け
   - プラットフォーム固有の問題にも対応

4. **自動化**
   - セットアップスクリプトで手順を簡略化
   - インタラクティブなセットアップも検討
   - Docker Composeでの環境統一

5. **動作確認**
   - ヘルスチェックエンドポイントの提供
   - 明確な成功基準
   - 自動テストでの検証

### 避けるべきこと

- ❌ 前提条件を書かない
- ❌ 手順が曖昧（「設定してください」だけ）
- ❌ エラーへの対処法がない
- ❌ プラットフォームの違いを無視
- ❌ 動作確認方法がない

### 次のステップ

次の章では、実践的なREADMEパターンを学びます。この章で学んだセットアップ手順を、
プロジェクトのタイプに応じてどのように適用するかを見ていきます。
```

## チェックリスト

### セットアップ手順のチェックリスト

ドキュメントを書き終えたら、以下を確認してください：

#### 前提条件

- [ ] 必要なツールとバージョンを明記した
- [ ] 前提知識を明示した
- [ ] プラットフォーム別の要件を区別した
- [ ] バージョン確認コマンドを提供した

#### 手順の明確性

- [ ] すべてのコマンドがコピペで実行できる
- [ ] 各ステップの目的を説明した
- [ ] 各ステップの成功を確認できる
- [ ] 実行時間の目安を提供した

#### トラブルシューティング

- [ ] よくあるエラーを列挙した
- [ ] エラーメッセージを正確に記載した
- [ ] 各エラーの解決策を明示した
- [ ] プラットフォーム固有の問題に対応した

#### 動作確認

- [ ] 動作確認の方法を明記した
- [ ] ヘルスチェックエンドポイントを提供した
- [ ] 成功基準が明確である
- [ ] 失敗時の対処法がある

#### メンテナンス性

- [ ] ドキュメントのバージョンを記載した
- [ ] 変更履歴を追跡している
- [ ] 定期的に検証する仕組みがある
- [ ] CI/CDでテストしている

#### その他

- [ ] リンク切れがない
- [ ] スクリーンショットが最新
- [ ] コマンド出力が最新
- [ ] タイポがない

すべてチェックできれば、優れたセットアップドキュメントの完成です！

---

**次の章**: [第7章: 実践的なREADMEパターン](./07-practical-readme-patterns.md)

セットアップ手順の書き方を習得したら、次はプロジェクトのタイプ別に
実践的なREADMEパターンを学びましょう。
