---
title: "QAメトリクス概要：データドリブンな品質管理"
---

# QAメトリクス概要：データドリブンな品質管理

## QAメトリクスの全体像

QAメトリクスは、品質状況を定量的に把握し、データに基づいた意思決定を行うための指標です。第3章で基礎を学びましたが、本章では実務での収集・分析・活用について詳しく解説します。

## 主要なQAメトリクス

### テスト実行メトリクス

```
テスト実行率 = 実行済みテストケース数 / 総テストケース数 × 100

テスト合格率 = 合格テストケース数 / 実行済みテストケース数 × 100

1日あたりのテスト実行数 = 総実行数 / 実行日数
```

### バグ関連メトリクス

```
バグ発見率 = 発見バグ数 / テストケース数

バグ修正率 = 修正済みバグ数 / 発見バグ数 × 100

再オープン率 = 再オープンバグ数 / クローズバグ数 × 100

バグ密度 = バグ数 / コード行数（KLOC）
```

### 効率性メトリクス

```
テスト効率 = 発見バグ数 / テスト実行時間

自動化率 = 自動化テストケース数 / 総テストケース数 × 100

カバレッジ = テスト済み機能 / 総機能 × 100
```

## メトリクス収集の自動化

### Jira APIからバグデータ取得

```python
# jira_metrics.py
from jira import JIRA
import pandas as pd

jira = JIRA(server='https://your-domain.atlassian.net',
            basic_auth=('email', 'api_token'))

# バグチケット取得
jql = 'project = PROJ AND type = Bug AND created >= -30d'
bugs = jira.search_issues(jql, maxResults=1000)

data = []
for bug in bugs:
    data.append({
        'key': bug.key,
        'severity': bug.fields.customfield_severity,
        'status': bug.fields.status.name,
        'created': bug.fields.created,
        'resolved': bug.fields.resolutiondate,
    })

df = pd.DataFrame(data)

# 深刻度別集計
severity_count = df.groupby('severity').size()
print(severity_count)
```

### GitHub Actionsからテスト結果取得

```yaml
# .github/workflows/collect-metrics.yml
name: Collect QA Metrics

on:
  workflow_run:
    workflows: ["Test Suite"]
    types: [completed]

jobs:
  collect-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Extract test results
        run: |
          echo "total_tests=${{ steps.test.outputs.total }}" >> metrics.txt
          echo "passed=${{ steps.test.outputs.passed }}" >> metrics.txt
          echo "failed=${{ steps.test.outputs.failed }}" >> metrics.txt

      - name: Send to metrics DB
        run: |
          curl -X POST https://metrics-api.example.com/test-results \
            -H "Content-Type: application/json" \
            -d @metrics.json
```

## メトリクスダッシュボードの設計

### ダッシュボードの階層構造

**エグゼクティブダッシュボード**（経営層向け）
- 品質KPI（健全性スコア）
- リリース準備状況
- 重大バグ数のトレンド

**QAマネージャーダッシュボード**
- テスト進捗状況
- バグトレンド（深刻度別）
- テストカバレッジ
- チーム生産性

**QAエンジニアダッシュボード**（日常業務用）
- 今日のテスト実行結果
- 未解決バグリスト
- テスト環境ステータス
- Flaky Testsアラート

## メトリクス分析の実例

### トレンド分析

```python
import matplotlib.pyplot as plt

# 週次バグ発見数のトレンド
weeks = ['W1', 'W2', 'W3', 'W4']
bugs_found = [45, 38, 52, 31]
bugs_fixed = [40, 42, 48, 35]

plt.plot(weeks, bugs_found, label='発見', marker='o')
plt.plot(weeks, bugs_fixed, label='修正', marker='s')
plt.xlabel('週')
plt.ylabel('バグ数')
plt.title('バグ発見・修正トレンド')
plt.legend()
plt.grid(True)
plt.show()
```

### 相関分析

```python
# コード変更量とバグ数の相関
import numpy as np

code_changes = [120, 250, 180, 300, 150]  # 行数
bugs = [2, 5, 3, 8, 3]

correlation = np.corrcoef(code_changes, bugs)[0, 1]
print(f'相関係数: {correlation:.2f}')
# 期待: 正の相関（変更量が多いほどバグも多い傾向）
```

## アラート設定

### しきい値ベースアラート

```yaml
# alerting-rules.yml
alerts:
  - name: high_bug_rate
    condition: bug_discovery_rate > 10  # 1日10件以上
    severity: warning
    notification: slack

  - name: critical_bugs
    condition: critical_bugs > 0
    severity: critical
    notification: pagerduty

  - name: test_failure_spike
    condition: failure_rate > 15%
    severity: warning
    notification: email

  - name: low_coverage
    condition: code_coverage < 70%
    severity: info
    notification: slack
```

## メトリクスの落とし穴と対策

### 1. バニティメトリクス（虚栄の指標）

**問題**: 見た目は良いが行動につながらない指標

```
悪い例: 「テストケース総数が5,000件」
→ 多ければ良いわけではない

良い例: 「Critical pathのカバレッジ95%」
→ 重要な部分の品質が分かる
```

### 2. ゲームの罠

**問題**: メトリクスが評価指標になると、数字を良く見せるための行動が発生

```
例: カバレッジを上げるために無意味なテストを追加
対策: カバレッジと品質の両方を見る
```

### 3. コンテキスト無視

**問題**: 数値だけを見て背景を考えない

```
例: 「バグ数が先月より増加」
→ テスト強化によって発見率が上がっただけかもしれない

対策: トレンドと背景を合わせて評価
```

## メトリクスレポートのベストプラクティス

### 週次QAレポートテンプレート

```markdown
# 週次QAレポート - 2026年3月第1週

## サマリー
- テスト実行率: 92% (目標: 90%)
- バグ発見: 28件 (前週比: +5件)
- Critical/High: 2件 → すべて修正完了

## トピックス
### ✅ 良かったこと
- E2Eテストの自動化が完了し、実行時間が50%短縮

### ⚠️ 注意事項
- 決済APIのFlaky Testが3件発生
- 来週優先で修正予定

### 📊 メトリクス
| 指標 | 今週 | 先週 | 目標 |
|------|------|------|------|
| テスト実行数 | 1,250 | 1,180 | 1,200 |
| カバレッジ | 84% | 82% | 85% |
| バグ修正率 | 95% | 92% | 90% |

## 次週の計画
- リグレッションスイートの整理
- パフォーマンステスト実施
```

## まとめ

QAメトリクスは品質を可視化し、データに基づいた意思決定を可能にします。メトリクス収集の自動化、適切なダッシュボード設計、トレンド分析により、継続的な品質改善が期待されます。

ただし、メトリクスは手段であり目的ではありません。数値の背後にある本質を理解し、チームで議論しながら改善アクションにつなげることが重要です。

次章では、バグメトリクスの詳細な分析手法について解説します。
