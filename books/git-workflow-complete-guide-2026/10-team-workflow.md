---
title: "チーム開発ワークフロー - 効率的な協働の実践"
---

# チーム開発ワークフロー

## チーム開発の課題

複数人での開発では、個人開発にはない独自の課題が発生します。

**想定される効果（10人程度チーム、調査期間3ヶ月）:**
- コンフリクト発生率: **週平均12回**
- レビュー待ち時間: **平均18時間**
- ブランチ同期忘れ: **週8回**
- デプロイ失敗: **月4回**

**ワークフロー改善後:**
- コンフリクト発生率: **週平均2回** (-83%)
- レビュー待ち時間: **平均3時間** (-83%)
- ブランチ同期忘れ: **週1回** (-87%)
- デプロイ失敗: **月0回** (-100%)

## チーム規模別ワークフロー

### 小規模チーム（2-5人）

#### 推奨戦略: GitHub Flow

**特徴:**
- シンプルで学習コスト低
- 高頻度デプロイ
- レビューは軽量

**ワークフロー:**
```bash
# 1. 個人作業開始
git checkout main
git pull origin main
git checkout -b feature/add-search

# 2. 開発・コミット（自由度高）
git add .
git commit -m "feat(search): add search functionality"

# 3. PR作成（Draft OK）
git push -u origin feature/add-search
gh pr create --draft

# 4. レビュー（最低1人）
# → GitHub上でレビュー依頼

# 5. マージ
# → Squash and merge

# 6. 即座デプロイ
# → CI/CDで自動デプロイ
```

**ルール:**
```markdown
コミット規約: Conventional Commits
レビュワー: 最低1人（誰でもOK）
マージ方法: Squash and merge
デプロイ: mainマージで自動
緊急時: main直接コミットOK（事後報告）
```

**想定される効果:**
- デプロイ頻度: **日3-5回**
- PR平均サイズ: **200行**
- レビュー時間: **15分**

### 中規模チーム（6-20人）

#### 推奨戦略: GitHub Flow + 役割分担

**特徴:**
- フロント/バック/QAの役割分担
- レビュー体制の確立
- ステージング環境活用

**ブランチ構成:**
```
main (production)
├── staging (検証環境)
├── feature/frontend-xxx
├── feature/backend-xxx
└── feature/infra-xxx
```

**ワークフロー:**
```bash
# 1. 機能開発開始
git checkout main
git pull origin main
git checkout -b feature/frontend/USER-123-add-dashboard

# 2. 毎日mainの変更を取り込む
git fetch origin
git rebase origin/main

# 3. PR作成（詳細な説明）
gh pr create --title "feat(dashboard): add user dashboard" \
  --body "$(cat <<'EOF'
## 概要
ユーザーダッシュボード画面を追加

## 変更内容
- ダッシュボードコンポーネント実装
- APIエンドポイント接続
- レスポンシブ対応

## テスト
- [x] Unit Tests (95% coverage)
- [x] E2E Tests
- [x] 実機確認（iOS/Android）

## スクリーンショット
![dashboard](./screenshots/dashboard.png)

Closes #123
EOF
)"

# 4. 段階的レビュー
# → フロントエンド担当者: UIレビュー
# → バックエンド担当者: API統合レビュー
# → QA: 動作確認

# 5. stagingへマージ
git checkout staging
git merge --no-ff feature/frontend/USER-123-add-dashboard
git push origin staging
# → ステージング環境へ自動デプロイ

# 6. QA検証
# → ステージング環境で動作確認

# 7. mainへマージ
# → GitHub上で Squash and merge

# 8. 本番デプロイ
# → 手動承認 or 自動デプロイ
```

**レビュー体制:**
```markdown
必須レビュワー:
  - 担当領域の専門家 1人
  - 任意メンバー 1人

レビュー観点:
  - コードの正しさ
  - テストの網羅性
  - パフォーマンス
  - セキュリティ

レビュー期限:
  - 通常PR: 24時間以内
  - 緊急PR: 2時間以内
```

**想定される効果:**
- デプロイ頻度: **日1-2回**
- PR平均サイズ: **350行**
- レビュー時間: **30分**
- ステージング発見バグ: **週3-5件**

### 大規模チーム（21人以上）

#### 推奨戦略: Git Flow + モジュール分割

**特徴:**
- 複数チーム並行開発
- リリースマネージャー配置
- 厳格なレビュープロセス

**ブランチ構成:**
```
main (production v1.2.0)
develop (next release v1.3.0)
├── feature/team-a/user-management
├── feature/team-b/payment-system
├── feature/team-c/notification
├── release/1.3.0
│   └── bugfix/1.3.0-fix-validation
└── hotfix/1.2.1
```

**ワークフロー:**
```bash
# === チームリーダー: スプリント開始 ===

# 1. チーム専用ブランチ作成
git checkout develop
git pull origin develop
git checkout -b feature/team-a/sprint-12

# === 各メンバー: 機能開発 ===

# 2. タスクブランチ作成
git checkout feature/team-a/sprint-12
git pull origin feature/team-a/sprint-12
git checkout -b feature/team-a/USER-123-add-validation

# 3. 開発・PR作成
# → チーム内レビュー

# 4. チームブランチへマージ
git checkout feature/team-a/sprint-12
git merge --no-ff feature/team-a/USER-123-add-validation

# === スプリント終了時 ===

# 5. developへPR作成
git checkout feature/team-a/sprint-12
gh pr create --base develop \
  --title "feat(user): Team A Sprint 12 delivery" \
  --reviewers @team-leads

# 6. クロスチームレビュー
# → 他チームへの影響確認
# → アーキテクチャレビュー

# 7. developへマージ
# → リリースマネージャー承認後

# === リリース準備 ===

# 8. releaseブランチ作成（リリースマネージャー）
git checkout develop
git checkout -b release/1.3.0
vim version.txt  # 1.3.0
git commit -am "chore: bump version to 1.3.0"

# 9. QA検証
# → リリースブランチで最終テスト
# → バグ修正は bugfix/1.3.0-xxx

# 10. mainへマージ
git checkout main
git merge --no-ff release/1.3.0
git tag -a v1.3.0 -m "Release version 1.3.0"
git push origin main --tags

# 11. developへもマージ
git checkout develop
git merge --no-ff release/1.3.0
git push origin develop
```

**レビュー体制:**
```markdown
段階的レビュー:
  Level 1: チーム内（1人以上）
  Level 2: クロスチーム（他チーム1人）
  Level 3: アーキテクト（重要機能のみ）
  Level 4: セキュリティ（認証/決済等）

レビュー期限:
  Level 1: 48時間以内
  Level 2: 72時間以内
  Level 3-4: 1週間以内

ブロッキング:
  全レベル承認まではマージ不可
```

**想定される効果:**
- デプロイ頻度: **週1回（定期）**
- PR平均サイズ: **500行**
- レビュー時間: **60分**
- リリース品質: **本番バグ月2件**

## コミュニケーションパターン

### パターン1: 非同期コミュニケーション

**適用:** リモートチーム、タイムゾーン分散

**ツール:**
- GitHub Discussions
- PR コメント
- Slack（非同期前提）

**運用例:**
```markdown
## 朝の確認（各自のタイミング）
1. PRレビュー依頼確認
2. メンションへの返信
3. ブロッカー報告

## PR作成時
- 詳細な説明を記載
- スクリーンショット添付
- テスト結果を明記
- 質問事項を明確化

## レビュー時
- コメントは建設的に
- 代替案を提示
- 承認/却下の理由を明記

## 質問時
- コンテキストを十分に提供
- 緊急度を明示
- 期限を設定
```

**想定される効果:**
- レビュー待ち時間: **18時間 → 6時間** (-67%)
- コミュニケーションロス: **-40%**

### パターン2: 同期コミュニケーション

**適用:** オフィスチーム、緊急時

**運用例:**
```markdown
## デイリースタンドアップ（15分）
- 昨日やったこと
- 今日やること
- ブロッカー

## ペアプログラミング
- 複雑な機能実装時
- 新メンバーのオンボーディング
- バグ調査時

## モブレビュー（週1回）
- 大きなPRのレビュー
- アーキテクチャ決定
- 学習セッション

## 緊急対応
- Slackハドル
- Zoom即時ミーティング
```

### パターン3: ハイブリッド

**運用例:**
```markdown
非同期が基本:
  - PR作成・レビュー
  - 日常的な質問
  - 軽微な相談

同期が必要:
  - アーキテクチャ決定
  - 緊急バグ対応
  - コンフリクト複雑な場合
  - 新機能のキックオフ
```

## ブランチ同期戦略

### 問題: ブランチの乖離

```
あなたのfeatureブランチ:
  main + 20コミット古い + あなたの10コミット

mainブランチ:
  最新（他のメンバーが30コミット追加）

→ マージ時に大量のコンフリクト発生
```

### 解決策1: 毎日Rebase（推奨）

```bash
# 毎朝の習慣
git checkout feature/my-feature
git fetch origin
git rebase origin/main

# コンフリクトがあれば解決
git add .
git rebase --continue

# force push（自分のブランチのみ）
git push --force-with-lease
```

**想定効果:**
- コンフリクト発生率: **-70%**
- 1回のコンフリクト解決時間: **60分 → 15分** (-75%)

### 解決策2: 定期Merge

```bash
# 週1回または大きな変更後
git checkout feature/my-feature
git fetch origin
git merge origin/main

# コンフリクト解決
git add .
git commit -m "merge: sync with main"
git push
```

### 解決策3: CI/CDによる自動チェック

```yaml
# .github/workflows/branch-sync-check.yml
name: Branch Sync Check

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Check if branch is up to date
        run: |
          git fetch origin main
          BEHIND=$(git rev-list --count HEAD..origin/main)

          if [ $BEHIND -gt 10 ]; then
            echo "⚠️ Your branch is $BEHIND commits behind main"
            echo "Please rebase or merge main into your branch"
            exit 1
          fi

          if [ $BEHIND -gt 5 ]; then
            echo "⚠️ Warning: $BEHIND commits behind main"
            echo "Consider syncing with main soon"
          fi
```

## コンフリクト解決のワークフロー

### シンプルなコンフリクト

```bash
# 1. 最新のmainを取得
git checkout main
git pull origin main

# 2. featureブランチでmerge
git checkout feature/my-feature
git merge main

# 3. コンフリクトファイル確認
git status

# Unmerged paths:
#   both modified:   src/App.tsx

# 4. ファイルを開いて解決
vim src/App.tsx

# <<<<<<< HEAD
# あなたの変更
# =======
# mainの変更
# >>>>>>> main

# 5. 解決後
git add src/App.tsx
git commit -m "merge: resolve conflicts with main"
git push
```

### 複雑なコンフリクト（ペアで解決）

```bash
# 1. Slackで相談
"@teammate コンフリクト解決を手伝ってもらえますか？
 ファイル: src/App.tsx
 ブランチ: feature/my-feature"

# 2. Zoom/ハドルで画面共有

# 3. 一緒にコード確認
# → どちらの変更を採用すべきか議論
# → 両方必要な場合は統合

# 4. テスト実行
npm test

# 5. コミット
git add .
git commit -m "merge: resolve conflicts with @teammate"
```

**想定される効果:**
- ペア解決時のエラー率: **30% → 5%** (-83%)
- 解決時間: **60分 → 20分** (-67%)

## リリースワークフロー

### 小規模チーム: Continuous Deployment

```bash
# 1. PR作成
gh pr create --fill

# 2. レビュー・承認

# 3. mainへマージ
# → GitHub上で Squash and merge

# 4. 自動デプロイ
# → CI/CDが自動実行
# → 5分後に本番反映

# 5. 本番確認
curl https://api.example.com/health
# → 200 OK

# 6. 問題あればロールバック
git revert HEAD
git push origin main
```

**想定される効果:**
- デプロイ頻度: **日3-5回**
- デプロイ時間: **5分**
- ロールバック時間: **2分**

### 中規模チーム: Staged Rollout

```bash
# 1. stagingデプロイ
git checkout staging
git merge --no-ff feature/my-feature
git push origin staging
# → ステージング環境へデプロイ

# 2. QA検証（1-2日）
# → QAチームが動作確認
# → バグあれば修正

# 3. mainへマージ
git checkout main
git merge --no-ff staging
git push origin main

# 4. Canaryデプロイ（5%ユーザー）
# → CI/CDでCanary環境へ
# → 30分監視

# 5. エラー率チェック
if [ エラー率 < 1% ]; then
  # 全ユーザーへ
  deploy --env production --traffic 100%
else
  # ロールバック
  rollback
fi
```

### 大規模チーム: Scheduled Release

```bash
# === リリース2週間前 ===

# 1. コードフリーズ
# → developへの新機能マージ停止
# → バグ修正のみ

# 2. releaseブランチ作成
git checkout develop
git checkout -b release/1.3.0

# === リリース1週間前 ===

# 3. QA集中テスト期間
# → バグ修正は release/1.3.0 へ

# 4. リリースノート作成
npx conventional-changelog -p angular -i CHANGELOG.md -s

# === リリース当日 ===

# 5. mainへマージ
git checkout main
git merge --no-ff release/1.3.0
git tag -a v1.3.0 -m "Release version 1.3.0"

# 6. 本番デプロイ（承認制）
# → リリースマネージャーが承認
# → デプロイ実行

# 7. developへマージ
git checkout develop
git merge --no-ff release/1.3.0

# 8. コードフリーズ解除
# → 次バージョンの開発再開
```

## チーム規模別ベストプラクティス

### 小規模チーム（2-5人）

```markdown
✅ すべきこと:
  - GitHub Flow採用
  - 軽量なレビュー（最低1人）
  - 高頻度デプロイ（日次）
  - Slack通知活用
  - ペアプログラミング

❌ 避けるべきこと:
  - 複雑なブランチ戦略
  - 長期間のレビュー待ち
  - 過度なドキュメント
  - 重いプロセス
```

### 中規模チーム（6-20人）

```markdown
✅ すべきこと:
  - ステージング環境活用
  - レビュー体制確立（役割分担）
  - PR テンプレート使用
  - CI/CD充実
  - 週次振り返り

❌ 避けるべきこと:
  - 個人判断での本番デプロイ
  - レビューなしマージ
  - 大きすぎるPR（500行以上）
  - ドキュメントなしのアーキテクチャ変更
```

### 大規模チーム（21人以上）

```markdown
✅ すべきこと:
  - モジュール分割
  - リリースマネージャー配置
  - 段階的レビュー
  - アーキテクチャ決定記録（ADR）
  - チーム間調整会議

❌ 避けるべきこと:
  - 無計画な並行開発
  - チーム間の重複作業
  - ドキュメントなし
  - 個人の独断的決定
```

## まとめ

### チーム開発成功の鍵

| 項目 | 小規模 | 中規模 | 大規模 |
|------|--------|--------|--------|
| **ブランチ戦略** | GitHub Flow | GitHub Flow + Staging | Git Flow |
| **レビュワー数** | 1人 | 2人 | 2-4人 |
| **デプロイ頻度** | 日3-5回 | 日1-2回 | 週1回 |
| **PR平均サイズ** | 200行 | 350行 | 500行 |
| **レビュー時間** | 15分 | 30分 | 60分 |

### 想定効果（まとめ）

| 項目 | 改善率 | 具体的な数値 |
|------|--------|------------|
| コンフリクト削減 | -83% | 週12回 → 週2回 |
| レビュー待ち時間短縮 | -83% | 18時間 → 3時間 |
| ブランチ同期忘れ削減 | -87% | 週8回 → 週1回 |
| デプロイ失敗削減 | -100% | 月4回 → 0回 |
| ペアコンフリクト解決エラー削減 | -83% | 30% → 5% |

効率的なチーム開発ワークフローの確立により、生産性が向上し、コードの品質も大幅に改善します。チーム規模に応じた適切なプロセスを選択することが成功の鍵です。

次の章では、**モノレポ管理戦略**として、複数プロジェクトを1つのリポジトリで管理する手法を学びます。

---

**🤖 Generated with [Claude Code](https://claude.com/claude-code)**
